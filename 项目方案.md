# 港美股量化交易监控系统设计方案 (C#)

## 0. 文档信息
*   **版本**: 1.0.0
*   **目标平台**: Ubuntu 22.04+ / .NET 8+
*   **核心组件**: Futu OpenAPI, QuantConnect LEAN SDK, NetMQ (ZeroMQ)
*   **适用场景**: 100+ 标的实时监控、日线/分钟级信号提醒、后期实盘扩展

---

## 1. 总体架构设计
本系统采用**发布/订阅 (Pub/Sub)** 架构模型，将数据采集与逻辑决策解耦。

### 1.1 逻辑分层
1.  **数据采集进程 (DataServer)**: 负责维护富途连接、数据库持久化及行情分发。
2.  **执行逻辑进程 (Execution)**: 负责指标计算、信号判断及消息推送。
3.  **共享库 (Core)**: 统一定义全系统的对象模型。

---

## 2. 项目工程结构 (Solution)

### 2.1 `Trader.Core` (Class Library)
**职责**: 系统的“通用语言”。
*   **数据模型**: 
    *   `TickData`: 原始 Tick。
    *   `BarData`: OCHL 结构。
    *   `Signal`: 触发信号的消息体。
*   **配置中心**: 存储端口号、数据库路径、Telegram Token 等。

### 2.2 `Trader.DataServer` (Worker Service)
**职责**: 系统的“水源”，保持 7x24 稳定运行。
*   **Futu Client**: 封装富途 C# SDK，处理断线重连。
*   **Storage**: 使用 **EF Core + SQLite** 异步写入原始 K 线。
*   **Broadcaster**: 使用 **NetMQ (Publisher)** 在 `tcp://*:5555` 广播行情。
*   **优势**: 逻辑极简，不涉及计算，极难崩溃。

### 2.3 `Trader.Execution` (Worker Service)
**职责**: 系统的“大脑”，负责计算和发出通知。
*   **Indicator Engine**: 
    *   集成 `QuantConnect.Indicators`。
    *   为每只股票动态创建指标字典：`Dictionary<string, List<IndicatorBase>>`。
*   **Cold Start**: 启动时自动从 SQLite 查询历史数据并调用 `indicator.Update()` 完成预热。
*   **Event Listener**: **NetMQ (Subscriber)** 实时监听行情并触发更新。
*   **Notifier**: 集成飞书/Telegram Webhook。

---

## 3. 核心技术细节

### 3.1 进程间通信 (IPC) 协议
使用 ZeroMQ 避免了频繁读写数据库带来的延迟：
*   **Topic**: `Quotes` (实时行情)
*   **Format**: JSON 或 Protobuf (追求极致性能选后者)

### 3.2 LEAN 指标本地化调用
无需运行 LEAN 的复杂环境，只需引用 NuGet 包：
```csharp
// 示例：在 Execution 进程中独立计算
var rsi = new RelativeStrengthIndex(14);
// 实时喂入数据
rsi.Update(dateTime, closePrice);
// 读取结果
var currentValue = rsi.Current.Value;